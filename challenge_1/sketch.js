function setup() {
	// create a place to draw
	createCanvas(640, 360);
	noStroke();
	noLoop();
}

function draw() {
	// clear the background
	background(50, 50, 150);

	// set a fill color
	fill(53, 142, 239);

//draw rect
rect(30, 20, 500, 500);
}
