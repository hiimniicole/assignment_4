function setup() {
	// create a place to draw
	createCanvas(640, 360);
	noStroke();
	noLoop();
}

function draw() {
	// clear the background
	background(150, 50, 150);

	// set a fill color
	fill(255, 255, 255);

	// draw the rect
	fill (86,86,86);
rect(30, 20, 55, 55);
}
